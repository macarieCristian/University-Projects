#include"Repository.h"
#include"Ui.h"
/*
int main()
{
	//testRepo();

	MedicationRepo *repo = createRepo();
	Controller *controller = createController(repo);

	//addMedicine(controller, "Paracetamol", 2.0, 10, 20);
	//addMedicine(controller, "Algocalmin", 3.0, 12, 30);
	//addMedicine(controller, "Nurofen", 1.0, 20, 50);
	//addMedicine(controller, "NurofenGripa", 4.0, 10, 70);
	//addMedicine(controller, "Forte", 1.5, 30, 30);

	UI *ui = createUI(controller);

	startUI(ui);

	destroyUI(ui);

	return 0;
}*/